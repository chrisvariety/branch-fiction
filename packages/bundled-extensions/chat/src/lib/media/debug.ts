export const DEBUG_MODE = false;

// Debug helper to display an image in the terminal using viu.
// Accepts either base64 string or raw bytes (Uint8Array).
export async function debugImage(
  _image: string | Uint8Array,
  _label: string
): Promise<void> {
  // TODO
  return;
}
